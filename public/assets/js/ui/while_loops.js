// Generated by CoffeeScript 1.10.0
var index, item, largest_index, letters, reversed;

letters = ['a', 'b', 'c', d];

reversed = (function() {
  var results;
  results = [];
  while (letters.length > 0) {
    results.push(letters.pop());
  }
  return results;
})();

console.log(reversed);

letters = ['a', 'b', 'c', 'd'];

reversed = (function() {
  var results;
  results = [];
  while (letters.length !== 0) {
    results.push(letters.pop());
  }
  return results;
})();

console.log(reversed);

letters = ['a', 'b', 'c', 'd'];

largest_index = letters.length - 1;

reversed = (function() {
  var i, len, results;
  results = [];
  for (index = i = 0, len = letters.length; i < len; index = ++i) {
    item = letters[index];
    results.push(letters[largest_index - index]);
  }
  return results;
})();

console.log(reversed);

//# sourceMappingURL=while_loops.js.map
